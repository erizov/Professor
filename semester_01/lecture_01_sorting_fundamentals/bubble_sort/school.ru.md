**Bubble Sort: простой алгоритм сортировки**

**Принцип работы:**
Bubble Sort - это простой алгоритм сортировки, который проходит по списку несколько раз, сравнивая соседние элементы и меняя их местами, если они находятся в неправильном порядке. Этот процесс повторяется до тех пор, пока список не будет отсортирован.

**Сложность:**
В худшем случае сложность алгоритма Bubble Sort составляет O(n^2), где n - количество элементов в списке.

**Применение на практике:**
Bubble Sort не является эффективным алгоритмом сортировки, поэтому чаще всего используется для обучения и понимания основных принципов сортировки.

**Сравнение с другими алгоритмами:**
Bubble Sort неэффективен на больших списках данных и сравнивается с более быстрыми алгоритмами, такими как Quick Sort и Merge Sort.

**Минимальный пример кода:**
```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n-i-1):
            if arr[j] > arr[j+1]:
                arr[j], arr[j+1] = arr[j+1], arr[j]
    return arr

arr = [64, 34, 25, 12, 22, 11, 90]
sorted_arr = bubble_sort(arr)
print(sorted_arr)
```

**Частые ошибки:**
- Необходимо быть внимательным при реализации условия сравнения элементов и обмена их местами.
- Не забывайте про выход за пределы списка при обращении к элементам.

**Рекомендуемая литература:**
- "Грокаем алгоритмы" Адитья Бхаргава
- "Алгоритмы. Построение и анализ" Томас Кормен, Чарльз Лейзерсон, Рональд Ривест, Клиффорд Штайн